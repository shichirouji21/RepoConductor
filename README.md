# RepoConductor
RepoConductor - A tool to manage multiple git repositories

Run `repoconductor` from a directory containing git repositories. It discovers git repositories recursively from the current working directory and lets you run git commands, inspect branch status, and tag repositories from the terminal UI.
